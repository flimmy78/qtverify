A1:正常运行状态
A2:查看参数，如表号、时间等
A3:查看历史数据
A4:检定状态

使用红外读表，每条指令前面都必须加唤醒指令
修改表号，必须在温差状态下

进入模拟流量状态，在流速(m3/h)状态下长按至A4
进入检定状态，在流量(m3)状态下长按至A4
退出检定状态，在流量(m3)状态下长按至A1
退出模拟流量状态，在流量(m3)状态下长按至A1

模拟流量状态，脉冲计数上限
DN25，常用流量，2.88 m3/h ？
检定状态的计数，在退出检定状态后会清零?
